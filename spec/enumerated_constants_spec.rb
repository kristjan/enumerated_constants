require 'spec_helper'

describe EnumeratedConstants do
  it 'has a version number' do
    expect(EnumeratedConstants::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
